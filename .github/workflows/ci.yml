name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test:unit
      - run: npm run test:security
      # The release workflow verifies these too; running them here as well stops a
      # component or integration break from surfacing only when a tag is pushed.
      - run: npm run test:integration
      - run: npm run test:component
